const headingStyles = {
  marginBottom: '20px',
};

const paraStyles = {
  marginBottom: '20px',
};

function Title() {
  return (
    <div>
      <h2 style={headingStyles}>SUBSCRIBE</h2>
      <p style={paraStyles}>
        Sign up with your email to receive news and updates.
      </p>
    </div>
  );
}

export default Title;
